name: Build Desktop Apps

on:
  push:
    branches: [ "main", "master" ,"dev-ci-workflow" ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: Windows-exe
            asset_name: MyApp-Windows.exe
          - os: macos-latest
            artifact_name: macOS-app
            asset_name: MyApp-macOS.app
          - os: ubuntu-latest
            artifact_name: Linux-AppImage
            asset_name: MyApp-Linux.AppImage

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # Linux system dependencies (GTK, Cairo, WebKit2Gtk)
      - name: Install Linux System Dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-gi \
            python3-gi-cairo \
            gir1.2-gtk-3.0 \
            gir1.2-webkit2-4.0 \
            gir1.2-webkit2-4.1 \
            libgirepository1.0-dev \
            libcairo2-dev \
            pkg-config \
            python3-dev \
            libfuse2t64 || sudo apt-get install -y libfuse2
        shell: bash

      # Install Python packages safely across OSes
      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pywebview pyinstaller pyinstaller-hooks-contrib
          if [ -f requirements.txt ]; then 
            pip install -r requirements.txt
          fi
        shell: bash

      # Windows Build
      - name: Build Windows Executable (.exe)
        if: matrix.os == 'windows-latest'
        run: |
          pyinstaller --noconsole --onefile --name "MyApp-Windows" --add-data "gui;gui" main.py

      # macOS Build
      - name: Build macOS Application (.app)
        if: matrix.os == 'macos-latest'
        run: |
          pyinstaller --noconsole --onedir --name "MyApp-macOS" --add-data "gui:gui" main.py

      # Linux AppImage Build with PyGObject Data Collection
      - name: Build Linux PyInstaller & AppImage
        if: matrix.os == 'ubuntu-latest'
        env:
          APPIMAGE_EXTRACT_AND_RUN: 1
        run: |
          # 1. Locate system typelib directory (where GTK and WebKit2 typelibs live on Ubuntu)
          export GI_TYPELIB_PATH=/usr/lib/x86_64-linux-gnu/girepository-1.0
          
          # 2. Build PyInstaller bundle forcing collection of gi and all typelibs
          pyinstaller --noconsole --onedir --name "MyApp" \
            --collect-all gi \
            --collect-all webview \
            --add-data "/usr/lib/x86_64-linux-gnu/girepository-1.0:gi_typelibs" \
            --add-data "gui:gui" \
            main.py

          # 3. Download appimagetool
          wget https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage -O appimagetool
          chmod +x appimagetool

          # 4. Create AppDir structure
          mkdir -p AppDir/usr/bin
          cp -r dist/MyApp/* AppDir/usr/bin/

          # 5. Create AppRun entrypoint WITH GI_TYPELIB_PATH set for runtime
          cat << 'EOF' > AppDir/AppRun
          #!/bin/sh
          HERE="$(dirname "$(readlink -f "${0}")")"
          export GI_TYPELIB_PATH="$HERE/usr/bin/gi_typelibs:$GI_TYPELIB_PATH"
          export LD_LIBRARY_PATH="$HERE/usr/bin:$LD_LIBRARY_PATH"
          exec "$HERE/usr/bin/MyApp" "$@"
          EOF
          chmod +x AppDir/AppRun

          # 6. Create desktop entry file
          cat << 'EOF' > AppDir/myapp.desktop
          [Desktop Entry]
          Name=MyApp
          Exec=AppRun
          Icon=myapp
          Type=Application
          Categories=Utility;
          EOF

          touch AppDir/myapp.png

          ARCH=x86_64 ./appimagetool AppDir MyApp-Linux.AppImage
          
      # Artifact Uploads
      - name: Upload Windows Artifact
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/MyApp-Windows.exe

      - name: Upload macOS Artifact
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/MyApp-macOS.app

      - name: Upload Linux Artifact
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: MyApp-Linux.AppImage
